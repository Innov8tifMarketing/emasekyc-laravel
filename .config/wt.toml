[[pre-start]]
copy = "wt step copy-ignored"

[[pre-start]]
env = "sed -i 's|^APP_URL=.*|APP_URL=https://{{ branch | sanitize }}.emasekyc-laravel.test|' {{ worktree_path }}/.env"
lerd = "cd {{ worktree_path }} && lerd link {{ branch | sanitize }}.emasekyc-laravel && lerd secure"

[[pre-start]]
database = "cd {{ worktree_path }} && touch database/database.sqlite && php artisan migrate:fresh --seed --no-interaction"
storage = "cd {{ worktree_path }} && php artisan storage:link --no-interaction"

[[pre-remove]]
lerd-unsecure = "cd {{ worktree_path }} && lerd unsecure || true"
lerd-unlink = "cd {{ worktree_path }} && lerd unlink || true"

[list]
url = "{% if branch == 'master' %}https://emasekyc-laravel.test{% else %}https://{{ branch | sanitize }}.emasekyc-laravel.test{% endif %}"
